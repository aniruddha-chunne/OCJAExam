package com.OCJA.chapter9_JavaAPI;

/**
 * 201 - JavaAPI
 * 599 - Answer
 *
 * 31. Which equivalent code can replace i -> i != 0 in the following
 * line?
 * Predicate<Integer> ip = i ‐> i != 0;
 * A. i -> { i != 0 }
 * B. i -> { i != 0; }
 * C. i -> { return i != 0 }
 * D. i -> { return i != 0; }
 *
 * 31. D. When you’re using brackets, both the return keyword and
 * semicolon are needed for the lambda to compile, making Option
 * D correct.
 *
 */

public class Que31_Imp {
    public static void main(String[] args) {
        
    }
}
